{
  "name": "u-nav-monorepo",
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install && npm install --prefix Backend && npm install --prefix WebDev",
    "dev:backend": "npm run dev --prefix Backend",
    "dev:frontend": "npm run dev --prefix WebDev",
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue,green\" \"npm run dev:backend\" \"npm run dev:frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
